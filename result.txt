<nav class="breadcrumbs">
  <div class="" id="nav-tab" role="tablist">
    <a class="active breadcrumbs__item is-active" 
       id="nav-home-tab{{i}}" 
       *ngFor="let approver of approvers; let i = index" <!-- Here is the issue -->
       (click)="activateTab(i)" 
       [class.active]="activeTab === i" 
       role="tab" 
       [attr.aria-controls]="'nav-home' + i" 
       aria-selected="true">
       Approver  {{i + 1}}
    </a>
  </div>
</nav>



<nav class="breadcrumbs">
  <div class="" id="nav-tab" role="tablist">
    <!-- Here is the corrected comment -->
    <a class="active breadcrumbs__item is-active" 
       id="nav-home-tab{{i}}" 
       *ngFor="let approver of approvers; let i = index" 
       (click)="activateTab(i)" 
       [class.active]="activeTab === i" 
       role="tab" 
       [attr.aria-controls]="'nav-home' + i" 
       aria-selected="true">
       Approver  {{i + 1}}
    </a>
  </div>
</nav>
