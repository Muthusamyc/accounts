<nav class="breadcrumbs">
  <div class="" id="nav-tab" role="tablist">
    <a class="active breadcrumbs__item is-active" *ngFor="let i of counter(data)" [class.active]="i === 1" [id]="'nav-home-tab'+i" data-bs-toggle="tab" [attr.data-bs-target]="'#nav-home' + i" role="tab" aria-controls="nav-home" aria-selected="true">Approver {{i}}</a>
  </div>
</nav>

<div class="tab-content" id="nav-tabContent">
  <div class="tab-pane fade show active" *ngFor="let i of counter(data)" [id]="'nav-home'+i" role="tabpanel" [attr.aria-labelledby]="'nav-home-tab'+i">
    <div class="container-field-approve">
      RFC initiate Approved
      <div class="border-rfc-heading">
        <label>Approver Name</label>
        <select [(ngModel)]="approver[i-1]">
          <option value="">{{supportapp}}</option>
          <option>{{supportapp1}}</option>
          <option>{{supportapp2}}</option>
          <option>{{supportapp3}}</option>
        </select>

        <div class="row mt-3">
          <div class="col">
            <label>Date</label>
            <input type="date" [(ngModel)]="today[i-1]" />
          </div>
          <div class="">
            <label>Remark</label>
            <textarea class="textarea-implement" [(ngModel)]="remark[i-1]"></textarea>
          </div>
          <div class="col"></div>
        </div>
      </div>
    </div>
    <div class="container-field-implement">
      <div>
        <label label-approve>Comments</label>
        <textarea class="textarea-implement" [(ngModel)]="comment[i-1]"></textarea>
      </div>
      <div>
        <label class="label-approve">Attach Document</label>
        <input class="input-approve" type="File" placeholder="" [(ngModel)]="attach[i-1]" />
      </div>
    </div>
    <div class="btn-sub-reset">
      <button class="btn-sub-approve" (click)="getData()">
        Approve
      </button>
      <button class="btn-sub-reject" type="submit" (click)="rejectbutton()">Reject</button>
      <button class="btn-sub" type="submit" [routerLink]="'/change-request'">Seek Clarification</button>
    </div>
  </div>
</div>
