<nav class="breadcrumbs">
  <div class="" id="nav-tab" role="tablist">
    <a class="active breadcrumbs__item is-active" 
       id="nav-home-tab{{i}}" 
       *ngFor="let i of counter(data)" 
       (click)="activateTab(i)" 
       [class.active]="activeTab === i" 
       role="tab" 
       [attr.aria-controls]="'nav-home' + i" 
       aria-selected="true">
       Approver  {{i}}
    </a>
  </div>
</nav>
<div class="tab-content" id="nav-tabContent">
  <!--tab one-->
  <div *ngFor="let i of counter(data)" class="tab-pane fade" [class.show]="activeTab === i" id="nav-home{{i}}" role="tabpanel">
    <!-- Tab content here -->
  </div>
</div>
