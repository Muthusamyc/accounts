selector(row:any) {
  this.selectradio = row;
  console.log(this.selectradio)
}
gotoapprove(tabId: string) {
  this.router.navigate(['/executive/{{this.selectradio.itcrid}}/edit'], { fragment: tabId });
  setTimeout(() => {
    const tabButton = document.getElementById(tabId + '-tab');
    if (tabButton) {
      tabButton.click();
    }
  });
  this.approvepage();
}
