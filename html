<!-- Pagination for first table -->
<pagination-controls (pageChange)="onPageChange($event)" [id]="config.id" style="margin-left:"></pagination-controls>

<!-- Pagination for second table -->
<pagination-controls (pageChange)="onPageChange1($event)" [id]="config1.id" style="margin-left:"></pagination-controls>
