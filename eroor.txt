config1: PaginationInstance = {
  id: 'pagination2', // Ensure this is initialized properly
  itemsPerPage: this.noOfRows1,
  currentPage: this.page1
};


<pagination-controls (pageChange)="onPageChange($event)" [id]="config1?.id"></pagination-controls>
