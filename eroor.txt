// Assuming this.lessonval contains the array you provided earlier

// Populate lessonsArray by filtering out empty text values
this.lessonval.forEach((item) => {
  const text = `all the lessons add here ${item.lessons}`;
  if (text.trim() !== '') {
    this.lessonsArray.push({ text });
  }
});
